---
topic: Employee salary management software (Incubyte take-home)
updated: 2026-07-16T17:59
---

- (decision) Working mode: Coaching path — user wants defensible reasoning they arrived at themselves, since scope/trade-off reasoning is the graded artifact
- (event) Persistent-fact glob project-context.md matched nothing; brief built purely from 'Incubyte THA.md' + '_job_desc.md'
- (event) Core open question flagged by facilitator and confirmed open by user: what 'answer questions about how the org pays people' means. Drives scope.
- (assumption) Brief has dual audience: HR manager persona in-fiction, Incubyte reviewer in fact. Reviewer is the one who grades scope reasoning.
- (event) Ran comp-analytics web research (Pave/Carta/Ravio/Workday/Deel); digest captured in addendum.md — metric formulas, multi-currency modeling, EU directive, pitfalls
- (event) Research finding that reframes scope: standard practice is bands-per-country evaluated in local currency, FX excluded from fairness math; normalizing to HQ currency is the #1 known modeling trap (phantom compa-ratio drift)
- (event) Pay band (min/mid/max + currency) is the load-bearing domain object all comp metrics hang off; THA seeds only employees, so whether bands exist is an open scope decision for rk
- (event) Awaiting rk's answers on: real pain (tedium vs unanswerable questions), who asks the questions, consequence of a wrong answer
- (decision) rk: real pain is TEDIUM (not unanswerable questions). HR manager is SELF-DIRECTED (no CFO/regulator forcing questions). Consequence of wrong answer is an AWKWARD SLACK MESSAGE (low stakes, not compliance exposure).
- (decision) rk: pay bands DO exist in ACME — bands are in scope as a domain object
- (event) TENSION flagged for rk: tedium + self-directed + low-consequence implies system-of-record product where analytics is decoration, but bands-in-scope implies someone asks whether people are paid correctly. Bands are the most expensive object in the domain and need motivation. Possible reconciliation: bands ARE the tedium (manual per-country VLOOKUP against band tables in Excel).
- (override) rk REVERSED earlier 'bands: yes' → NO PAY BANDS. Resolves the tension in favor of the tedium/self-directed/low-stakes framing. Consequence: compa-ratio, range penetration, market index, merit matrix all fall out of scope (all require min/mid/max). Comparison basis must become peer-relative/statistical rather than band-relative.
- (decision) Strongest available exclusion rationale for bands: bands require market benchmark data ACME does not have; seeding invented bands = fabricating the most load-bearing input in the domain, and every derived metric would inherit the fiction. Deriving structure statistically from actual salaries is honest about what data exists.
- (override) rk CORRECTED facilitator's band-exclusion rationale: salary data is ALSO seeded dummy data, so 'bands are fiction' is not a valid distinction. Rationale replaced (see next entries).
- (decision) REVISED band-exclusion rationale (2 legs): (1) derived-vs-asserted — a peer-group median is a TRUE fact about rows in the DB and yields meaningful deterministic unit tests; a compa-ratio vs an invented midpoint has no referent and its test is a tautology. (2) maturity assumption — maintaining per-country bands implies a comp function w/ survey data; THA's 'Excel + tedious' signals a low-maturity org. Salaries can be assumed to exist (they pay people); bands cannot.
- (decision) PEER GROUP DEFINED: two employees are peers iff same role + same level + same country. Consequence: comparison is intra-country therefore intra-currency; FX never enters the fairness math (matches researched best practice). Employee record needs at minimum: role, level, country, currency, salary.
- (decision) GENDER is in the employee record. Pay equity in scope.
- (decision) THIN CELLS: threshold n>=5 to answer; always display n; REFUSE below threshold rather than widening peer group (widening across countries would reintroduce FX into fairness math). Density guaranteed by SEED DESIGN, not by lowering the bar. Deliberately seed a few thin cells so the graceful-refusal path is demoable — 'product that knows what it does not know' is the judgment signal.
- (decision) KEY INSIGHT — peer group IS the regression control: gap computed within (role+level+country) is already controlled for job/seniority/geography, giving the 'adjusted' pay-gap view WITHOUT regression. Structural controlling at the schema replaces statistical controlling.
- (decision) COROLLARY — peer view is structurally BLIND to gender clustering across levels (level is controlled away), so a separate org-wide distribution view (gender across levels) is required. This is the unadjusted(shape)/adjusted(practice) pair in buildable, deterministically-testable form. Seed BOTH effects: within-peer gaps in some cells + women clustered at lower levels.
- (decision) SEED DESIGN (facilitator-recommended, rk asked): log-normal within cell (right-skew, makes mean-vs-median meaningful); country cost-of-LABOR multipliers; ~15-20% level progression (avoids level inversion); planted high/low outliers; deliberately thin cells. Seed script framed as a DESIGN ARTIFACT that determines whether the product has anything to say — not plumbing.
- (decision) rk: SALARY HISTORY IS IN. Salary becomes a time-series, not a column on employee: append-only salary records with effective_from. Current salary = latest record where effective_from <= today. Raises append rather than overwrite. Consequences: every analytic view has implicit 'as of today' semantics; unlocks 'who has not had a raise in N months' (strongly motivated by the self-directed tedium persona).
- (decision) HISTORY BOUNDED to avoid effective-dating sinkhole: NO future-dated/scheduled changes, NO approval workflow, NO retroactive-correction semantics (corrections would be new records). Append-only log only. Facilitator-proposed bound — rk to confirm at review.
- (decision) rk gave GO to draft the brief.
- (change) Applied doc_standards passes. Prose review: fixed dangling fragment, garden-path, zeugma, ambiguous 'whose', question punctuation, weak causal link. CORRECTED A REAL OVERCLAIM — 'gender gap within peer group isolates pay practice' claimed controls the schema does not deliver (no tenure/performance/hire-cohort control); now scoped to 'across those factors'. Softened 'best-documented trap' -> 'well-documented'.
- (change) Structure review: brief was ~1547 words (~3pp) vs 1-2pp target. Cut 'No pay bands' from Design Decisions (95w duplicate of exclusion-table row) and folded its one additive idea (derived-vs-asserted + testability) INTO the table row, strengthening the graded section. Merged 'Who This Serves' into Exec Summary (preserving 'she is the only user', which two exclusion rows depend on). Merged history bounds into Solution. Moved seed distribution params to addendum. Preserved Scope table and Problem's 4 bullets (Success Criteria cross-references them).
- (change) Added 'Seed design parameters' section to addendum.md to make the brief's pointer true — log-normal, cost-of-labour multipliers, 15-20% progression, planted outliers, thin cells, two gender effects, grid sizing math.
- (decision) rk CONFIRMED the facilitator-proposed history bounds (previously unconfirmed): append-only, NO future-dating, NO scheduled changes, NO approval workflow, NO retroactive correction. A correction appends a new record; the superseded value remains visible in the timeline. Accepted as a stated trade-off.
- (event) Memlog audit completed with rk: decisions -> brief; research + seed params -> addendum; working mode/missing project-context/go-ahead/editorial entries -> process noise; superseded band decisions retained in memlog as audit trail only. rk has edits pending before finalize; brief stays status: draft.
- (change) rk CAUGHT a contradiction with source text: THA says 'ACME org's HR TEAM manages salary data' but brief asserted 'She is also the only user.' Resolution (rk chose): keep single persona — THA's Persona section explicitly names 'HR Manager of the org' and desired-state says 'We want the HR manager to manage' — but REPAIR the reasoning. Exec Summary now acknowledges the team exists; auth exclusion reworded from 'no second role to protect anything from' (contradicted by source, and reads as naive for 10k salary records) to 'deferred, not dismissed — first thing needed before touching a real salary record'; new explicit ASSUMPTION names the team-vs-manager tension as the first to revisit.
- (decision) rk resolved all 6 open items: (1) data entry per-record via UI AND bulk import from spreadsheets — import moves from unaddressed to IN SCOPE, it is the truest answer to the tedium pain; (2) sole-user assumption stands unchanged; (3) salary = base/annual/gross/local currency CONFIRMED, plus new requirement: local currency must always be VISIBLE, never implicit; (4) gender value set = MALE, FEMALE, OTHERS; (5) n>=5 threshold confirmed; (6) role and level are SEEDED REFERENCE TABLES, not free text.
- (event) NEW OPEN raised by the interaction of (1) and (6): bulk import + reference-table taxonomy means an import row with an unknown role/level must either reject or map. This is the domain's job-matching problem at the import boundary — a silently-accepted variant ('Sr. Engineer' vs 'Senior Engineer') splits a peer group and corrupts every median derived from it, undetectably. rk to decide.
- (event) Noted re gender OTHERS: will fall below the n>=5 peer threshold nearly everywhere, so the refusal path (not silent omission) is what handles it. Recorded in brief.
- (override) rk REVERSED gender value set: MALE/FEMALE/OTHERS -> MALE/FEMALE only. Facilitator noted once that the threshold problem was about ANALYSIS while removing the value is about REPRESENTATION (schema can no longer represent a non-binary employee at all) and that facilitator may have nudged this by raising the threshold point; rk confirmed. Brief now states the simplification and its cost explicitly rather than letting a binary field read as unconsidered — deliberate scope, flagged as a limitation of the model, with the note that a production system would widen the field and let the threshold handle the analysis.
- (decision) CLARIFIED (rk asked): gender is NOT part of peer identity — male and female in the same role+level+country are ONE peer group. This is load-bearing: it is precisely what makes the gap computable within a group (compare median(male) vs median(female) inside one cell). If gender split the group there would be no shared basis and regression would be required.
- (decision) HOLE FOUND via rk's question — TWO thresholds needed, only one was specified. n>=5 governs PEER GROUP SIZE (trust the median). The gender gap additionally needs enough of EACH gender: a group of 8 at 7 men / 1 woman clears n>=5 but yields median-of-one vs median-of-seven — exactly the confident lie the threshold exists to prevent. rk DECIDED: require n>=5 PER GENDER for a gap to report. Skewed cells are the norm so this refusal is a common path, not an edge case.
- (decision) SEED CONSEQUENCE of the gap sub-threshold: cells carrying planted within-peer gender gaps need >=5 men AND >=5 women (>=10 people, balanced) or the view refuses and the demo shows nothing. TENSION: clustering (effect 2) deliberately skews gender by level, and skew starves the sub-threshold — so seed the two gender effects into DIFFERENT cells. Captured in addendum.
- (decision) rk DECIDED the last open — IMPORT BOUNDARY: bulk import REJECTS rows whose role/level is not in the reference table, reporting them rather than mapping/guessing. Rationale: rejection is the only option that cannot silently corrupt a peer group; a wrong auto-match is invisible and splits a peer group, making every derived median quietly incorrect. Deliberately the less friendly choice. Facilitator read it as PER-ROW rejection with a report (not per-file) so one typo does not lose a 10k-row import — flagged to rk for correction.
- (event) ALL OPEN QUESTIONS NOW CLOSED. Brief has no remaining Open items — only [ASSUMPTION] (sole user; salary definition) and [DECIDED] entries.
- (change) Second structure pass applied: 1836 -> 1494 words. Cut duplicated [DECIDED] restatements of Solution prose, condensed Solution (regression justification duplicated the protected exclusion row; worked examples live in addendum), merged Design Decisions into Solution, condensed gender decision. REJECTED the review's largest recommendation (Exec Summary 209->40 words): an assessment reviewer skims first and the summary is the only thing a skimmer reads — condensed to ~120 instead, keeping the Excel hook. Accepted reviewer's own conclusion that Scope at ~39% of the brief is the correct shape for a doc graded on scope reasoning; did not shave the exclusion table to hit a round word count.
- (decision) rk: status draft -> FINAL. Brief finalized.
- (decision) rk: _job_desc.md gitignored (personal application context, not part of the solution; repo is shared with Incubyte). 'Incubyte THA.md' committed as the problem statement every decision traces back to. Commit made directly to master, not a branch — the THA grades commit history as the evolution story, so a linear trail is the deliverable.
- (override) rk: omit the Claude co-author trailer from commit messages. Applied.
