{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "prisma generate && prisma migrate deploy && next build",
  "ignoreCommand": "if [ -z \"$VERCEL_GIT_COMMIT_REF\" ] || [ \"$VERCEL_GIT_COMMIT_REF\" = \"master\" ] || [ -n \"$CI_DRIVEN_PREVIEW\" ]; then exit 1; else exit 0; fi"
}
