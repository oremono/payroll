# `src/adapters/db/`

Prisma client + repository implementations (implementing `application/ports/`). Empty seam in
Story 1-1; populated from Story **1-3** (data model & migrations) onward.

Import rule (inherited from the adapters layer): **`application`, `domain`**.
